实现队列，是通过将一个数组看为循环的数组，也就是通过对下标的操作，满之后如果要增就清零来实现
这么这个数组就算是循了。
那么怎么判断数组是满的呢？
所以这个isFull 我们直接用size来判断是否已满，当size == capacity的时候，就满
然后这个队列的增加与减少
队列是遵循队尾入，队头出，
用数组表示就是（注：这只是一种实现情景，你同时减也可以，只要下标操作是同向进行就可）
入：rear增加，并且赋值。
出：front增加，并且返回增加前数组下标对应的值。