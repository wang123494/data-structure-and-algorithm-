现在来写链表的吧，链表，就是通过指针将不同的某结构体变量链接，主要考虑为NULL，这个玩意一不小心就会造成内存的非法访问，只能说指针太操蛋了，
然后链表的内容就那么点，实现基本的增删改查，一般都是通过链来遍历找对应的内容，需要注意这里有一个问题：关于malloc和free，他们只能对heap里的内容进行操作，而不能对stack的变量进行操作，所以我在main里声明了一个结构体变量，之后对其进行free的时候出现了invalid heap pointer的错误，然后之后有个哥告诉我 我的create funciton也有问题，不能创造最开始的链表，我可以加一个创造最先的链表的函数 还是很简单的。


